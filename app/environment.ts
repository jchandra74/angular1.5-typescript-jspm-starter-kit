const buildEnvironment: string =  "DEVELOPMENT";

const DEV_HTTP_LONG_DELAY_MS: number = 1000;
const DEV_HTTP_SHORT_DELAY_MS: number = 500;

export { DEV_HTTP_LONG_DELAY_MS, DEV_HTTP_SHORT_DELAY_MS }

export default buildEnvironment;